# .dh/skill_discovery.yaml
# Generated by /dh:setup-skill-discovery wizard (AUTO_MODE)
# Review and adjust entries as needed.
#
# Stack signals detected: TypeScript (package.json, typescript devDependency),
# Node.js >=24 ESM library/CLI (textlint rule preset), vitest test runner,
# oxlint + prettier for lint/format, zod for schema validation.
# No installed or marketplace-available skills matched these signals at generation time
# (npx skills list -g returned only generic document/utility skills, no
# TypeScript/Node/vitest-specific skill package). Populate skill_rules below if/when
# a relevant skill becomes available.

skill_discovery: auto   # auto | suggest | off

always_use_skills:      # AUTO_MODE: review and adjust as needed
  []

prefer_skills:          # AUTO_MODE: review and adjust as needed
  []

avoid_skills:           # AUTO_MODE: review and adjust as needed
  []

skill_rules:            # AUTO_MODE: review and adjust as needed
  []
